import React from 'react';

const Profile = ({playerGames, setCheck}) => {

  return (
    <div className="mobile-container">
      profile status
      {/* {playerGames[0].player} */}
    </div>
  )
};

export default Profile;